% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_mod_reads.R
\name{trim_mod_reads}
\alias{trim_mod_reads}
\title{Trim methylation calls at the read ends in a mod database}
\usage{
trim_mod_reads(
  mod_db,
  by_frac = 0.1,
  trim_start = FALSE,
  trim_end = TRUE,
  input_table = "calls",
  output_table = "calls_trimmed",
  samples = NULL
)
}
\arguments{
\item{mod_db}{An object identifying the mod database, as accepted by
\code{ModSeqR:::.modhelper_connectDB()} (e.g. a file path or an
existing mod database object).}

\item{by_frac}{Numeric scalar between 0 and 1 (exclusive) giving the
fraction of the read length to trim from each enabled end.}

\item{trim_start}{Logical; whether to trim from the start (low
\code{read_position}) of each read.}

\item{trim_end}{Logical; whether to trim from the end (high
\code{read_position}) of each read.}

\item{input_table}{Character scalar; name of the input table
containing methylation calls (default \code{"calls"}). Must contain
at least \code{read_position} and \code{read_length} columns.}

\item{output_table}{Character scalar; name of the output table to
create/overwrite with the trimmed calls (default \code{"calls_trimmed"}).}

\item{samples}{Character vector of sample name(s) to trim. If \code{NULL}
(default), trims all samples. Requires a \code{sample_name} column in
\code{input_table}.}
}
\value{
Invisibly returns the updated \code{mod_db} object, with
  \code{current_table} set to \code{output_table}.
}
\description{
Creates a new table in a mod database that keeps only methylation calls
within a central fraction of each read. Optionally restricts trimming to
one or more sample(s) via the \code{samples} argument.
}
